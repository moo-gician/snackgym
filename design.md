# B.E.A.S.T. 프리미엄 디자인 시스템 (Design System)

## 1. 디자인 철학 (Design Philosophy)
*   **Brutal & Immersive (잔혹한 몰입감):** 스파르타의 전장처럼 묵직하고 어두운 배경(#0A0A0C)을 사용하여 깊이감(Depth)과 입체감을 살리고, 사용자의 눈의 피로를 최소화합니다.
*   **Cognitive Balance (인지 부조화 방지):** 붉은색(Red)은 시각적 피로도와 에러(Error)의 의미를 강하게 주므로, 목표 달성이나 긍정적 피드백에는 붉은색 대신 **Aged Bronze**를 주력으로 사용합니다. 붉은색은 화면의 5~10% 이내의 아드레날린 포인트로만 제한합니다.
*   **Safe & Aggressive Animations:** 앱스토어 리젝 사유가 될 수 있는 폭력적인 '피 튀기는' 애니메이션은 배제하고, 무거운 청동 방패가 부딪치며 불꽃이 튀는 **스파크(Spark)**나 전사의 심장이 뛰는 묵직한 **맥박 펄스(Heartbeat Pulse)** 애니메이션으로 타격감을 구현합니다.
*   **Less, but better**: 장식적인 요소(대시보드 남용, 보더 강조, 그리드 배경)를 철저히 배제하고 본질적인 유틸리티에만 집중.

## 2. 컬러 팔레트 (Color Palette - Tailwind 매핑)
- **배경 (Background):** `abyss` (#0A0A0C) - 순수 블랙이 아닌 깊은 차콜 그레이 톤으로 엘리베이션(Shadow) 표현 최적화
- **표면 (Surface):** `charcoal` (#16161A) - 낡고 단단한 철갑 느낌의 다크 그레이
- **포인트 (Primary):** `bronze` (#C89A51) - 청동 방패의 질감을 띈 골드/브론즈 (목표 달성, 진행 바, 주요 액션 버튼)
- **위험/강조 (Accent):** `blood` (#D91A1A) - 강렬한 핏빛 레드 (비율 5~10% 이내로 제한, 핵심 경고나 엣지 디자인 요소)
- **텍스트 (Text):** 
  - `bone`: (#F0EFEA) - 거칠지만 또렷한 화이트 (Heading 등 주요 텍스트)
  - `ash`: (#8A8A93) - 가독성을 위한 재(Ash) 색상 (Body 보조 텍스트)

## 3. 타이포그래피 (Typography) 사용 영토 제한
*   **Display Font (`Oswald`):** 크고 두꺼운 강렬한 폰트는 **대형 정적 타이틀(Header) 및 거대한 목표 숫자**에만 제한적으로 사용하여 시각적 타격감을 줍니다.
*   **Body & Timer Font (`Inter`):** 숫자가 빠르게 변하는 운동 타이머, 세트 카운트 UI, 가이드라인 설명 등은 가독성 저하를 막기 위해 고정폭이 지원되는 **모던 산세리프(`Inter`)**를 철저히 유지합니다.

## 4. 핵심 UI 컴포넌트 & 마이크로 인터랙션

### A. 버튼 (Action Buttons)
*   일반적인 상태에서는 `charcoal` 배경을 사용하고, 활성화/선택 시 `border-bronze` 또는 `border-blood` 테두리와 함께 내부 글로우 효과를 줍니다.

### B. 듀얼 바디 맵 플립 카드 (Body Map Flip)
*   **형태**: 전면(가슴/어깨)과 후면(등/삼두)을 한 장의 카드로 앞뒤로 뒤집는 싱글 뷰 UI.
*   사용자가 근육 위로 마우스를 올리면 서서히 `blood` 혹은 `bronze` 컬러로 차오르는 효과.

### C. 세션 컨트롤러 (스피너 & 체크버튼)
*   **스피너 `[- 15 kg +]`**: 
    *   버튼 터치 영역을 엄지손가락에 맞게 충분히 크게 확보 (최소 `44px` 높이).
    *   클릭 시 짧고 탄력 있는 스케일 애니메이션(`scale(0.95)`)으로 즉각적인 타격감 제공.
*   **체크 버튼 `[✅]`**:
    *   완료 시 심장 박동(Pulse) 애니메이션 작동.
