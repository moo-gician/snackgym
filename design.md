# SnackGym 프리미엄 디자인 시스템 (Design System)

## 1. 디자인 철학 (Design Philosophy)
*   **Less, but better**: 장식적인 요소(대시보드 남용, 보더 강조, 그리드 배경)를 철저히 배제하고 '루틴 체크'라는 본질적인 유틸리티에만 집중.
*   **Function-Driven**: "1분 안에 앱을 켜고 운동을 체크한 뒤 나간다"는 마찰 제로 UX를 달성하기 위해 가장 굵고 명확한 타이포그래피와 직관적인 인터랙션 사용.
*   **Responsive & Alive**: 모든 탭, 클릭, 스피너 동작에 유려한 마이크로 애니메이션과 호버 효과를 부여하여, 단순한 웹앱이 아닌 고급 네이티브 앱을 쓰는 듯한 프리미엄 감각 제공.

## 2. 컬러 팔레트 (Color Palette - HSL)
빛과 생명력을 상징하는 맑고 투명한 'Summer Forest Morning' 테마.

*   **배경 (Background)**
    *   `Base`: HSL(120, 33%, 98%) — 맑은 아침 이슬 화이트 (탁한 베이지 배제)
    *   `Surface`: HSL(0, 0%, 100%) — 순백색 카드 표면
*   **포인트 (Primary & Accent)**
    *   `Primary`: HSL(127, 62%, 52%) — 햇살을 머금은 네온 라임/그린 (버튼, 활성화 상태)
    *   `Primary Hover`: HSL(127, 60%, 46%) — 깊이감 있는 짙은 그린
    *   `Accent`: HSL(190, 76%, 40%) — 맑은 피톤치드 틸 (보조 버튼, 스트릭 포인트)
*   **텍스트 (Typography Colors)**
    *   `Heading`: HSL(0, 0%, 10%) — 강렬하고 또렷한 챠콜 블랙
    *   `Body`: HSL(0, 0%, 40%) — 부드럽고 가독성 높은 그레이
    *   `Muted`: HSL(0, 0%, 65%) — 비활성화 및 보조 텍스트

*(금지 규칙: 퍼플/바이올렛 계열 절대 사용 금지, 강렬한 컬러 보더(테두리) 사용 금지)*

## 3. 타이포그래피 (Typography)
최대 가독성과 세련됨을 위해 Google Fonts의 모던 산세리프 적용 (Inter 또는 Pretendard 권장).

*   **Display / Header**: 자간(Letter-spacing)을 좁게(`-0.02em`) 설정하여 단단하고 고급스러운 타이틀 구성.
*   **Body**: 넉넉한 행간(`1.6`)으로 여백의 미(Whitespace) 극대화.
*   **금지 규칙**: 비례가 안 맞는 거대한 텍스트, 그라데이션이 들어간 키워드 텍스트 절대 금지.

## 4. 핵심 UI 컴포넌트 & 마이크로 인터랙션

### A. 듀얼 바디 맵 플립 카드 (Body Map Flip)
*   **형태**: 전면(가슴/어깨)과 후면(등/삼두)을 한 장의 카드로 앞뒤로 뒤집는 싱글 뷰 UI.
*   **인터랙션**: 
    *   `transform: rotateY(180deg)`를 활용한 부드러운 3D 플립 애니메이션 (0.6s 큐빅 베지어).
    *   사용자가 근육(SVG 경로) 위로 마우스를 올리면 서서히 `Primary` 컬러로 차오르는 글로우(Glow) 호버 효과.
    *   선택된 근육은 명확하게 점등되며 잔잔한 펄스(Pulse) 애니메이션 작동.

### B. 스낵 타이머 카드 (Session Duration Cards)
*   온보딩의 세션 시간 선택 3종 카드 (1분 / 3분 / 5분).
*   **인터랙션**: 
    *   hover 시 카드가 미세하게 위로 떠오름 (`transform: translateY(-4px)`).
    *   은은한 드롭 섀도우 확장. 텍스처 없는 매끄러운 표면 유지 (플랫 베이스에 깊이감만 부여).
    *   카드 내부에 중첩된 작은 카드(Bento-box 스타일) 남용 금지.

### C. 세션 컨트롤러 (스피너 & 체크버튼)
*   **스피너 `[- 15 kg +]`**: 
    *   버튼 터치 영역을 엄지손가락에 맞게 충분히 크게 확보 (최소 `44px` 높이).
    *   클릭 시 짧고 탄력 있는 스케일 애니메이션(`scale(0.95)`)으로 즉각적인 타격감 제공.
*   **체크 버튼 `[✅]`**:
    *   운동 완료 시, 체크 아이콘이 팝(Pop)하며 나타나고 카드의 배경색이 부드럽게 완료 상태(연한 Primary)로 트랜지션 됨.

## 5. 반응형 룰 (Fluid Responsiveness)
*   단순한 미디어 쿼리(Breakpoints) 분기를 넘어, 컨테이너 크기에 맞춰 내부 요소(폰트, 여백, 아이콘)가 `clamp()` 함수를 통해 유체처럼(Fluid) 매끄럽게 리사이징되도록 구현.
*   모바일 환경에서 불필요한 콘텐츠 레이아웃 시프트(CLS) 원천 차단.
